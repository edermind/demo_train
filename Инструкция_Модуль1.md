# Шпаргалка — Модуль 1. Настройка сетевой инфраструктуры

**Стенд:** ISP / HQ-SRV / BR-SRV / HQ-CLI — РедОС (Linux), HQ-RTR / BR-RTR — Eltex ESR.
**Домен:** `sirius-exam.org` · **Пароль учёток:** `P@ssw0rd` · **SSH-порт:** `2026`

> ⚠️ Веди отчёт по ходу: по каждому пункту со словами «занесите в отчёт» — отдельный документ со скриншотами. Итог сохрани как `ФамилияМодуль1`.

---

## 0. План адресации (зафиксируй сразу → Таблица 2)

| Сеть | Подсеть / маска | Шлюз (роутер) | Хост |
|------|-----------------|---------------|------|
| HQ-SRV — VLAN 100 (≤32) | `192.168.0.0/27` | `192.168.0.1` | HQ-SRV `192.168.0.2` |
| HQ-CLI — VLAN 200 (≥16) | `192.168.0.32/27` | `192.168.0.33` | HQ-CLI — по DHCP |
| Управление — VLAN 999 (≤8) | `192.168.0.64/29` | `192.168.0.65` | — |
| BR-SRV (≤16) | `192.168.1.0/28` | `192.168.1.1` | BR-SRV `192.168.1.2` |
| ISP ↔ HQ-RTR (задано) | `172.16.1.0/28` | ISP `.1` | HQ-RTR `.2` |
| ISP ↔ BR-RTR (задано) | `172.16.2.0/28` | ISP `.1` | BR-RTR `.2` |
| Туннель HQ↔BR | `10.10.10.0/30` | HQ `.1` / BR `.2` | — |

---

## 1. Имена устройств (FQDN)

**РедОС (ISP, HQ-SRV, BR-SRV, HQ-CLI):**
```bash
sudo hostnamectl set-hostname isp.sirius-exam.org      # на ISP
sudo hostnamectl set-hostname hq-srv.sirius-exam.org   # на HQ-SRV
sudo hostnamectl set-hostname br-srv.sirius-exam.org   # на BR-SRV
sudo hostnamectl set-hostname hq-cli.sirius-exam.org   # на HQ-CLI
exec bash   # перечитать приглашение
```

**Eltex (HQ-RTR / BR-RTR):**
```
configure
hostname HQ-RTR
exit
```

---

## 2. ISP — интернет, адресация, NAT (РедОС)

> Интерфейсы: `enp7s1` = WAN (DHCP). `enp7s2`/`enp7s3` = к HQ-RTR и к BR-RTR.
> Проверь по MAC в Proxmox, какой куда. Ниже: enp7s2→HQ, enp7s3→BR (поправь при необходимости).

```bash
# WAN — оставить на DHCP (обычно уже так)
sudo nmcli con mod enp7s1 ipv4.method auto

# В сторону HQ-RTR (172.16.1.0/28)
sudo nmcli con mod enp7s2 ipv4.method manual ipv4.addresses 172.16.1.1/28
sudo nmcli con up enp7s2

# В сторону BR-RTR (172.16.2.0/28)
sudo nmcli con mod enp7s3 ipv4.method manual ipv4.addresses 172.16.2.1/28
sudo nmcli con up enp7s3
```
> Если имя соединения ≠ имени интерфейса — смотри `nmcli con show`.

**Включить форвардинг + PAT (NAT) для офисов:**
```bash
# форвардинг
echo 'net.ipv4.ip_forward = 1' | sudo tee /etc/sysctl.d/99-fwd.conf
sudo sysctl -p /etc/sysctl.d/99-fwd.conf

# маскарад на выход в интернет (WAN = enp7s1)
sudo firewall-cmd --permanent --direct --add-rule ipv4 nat POSTROUTING 0 -o enp7s1 -j MASQUERADE
sudo firewall-cmd --permanent --add-masquerade
sudo firewall-cmd --reload
```
> Маршрут по умолчанию ISP получает по DHCP с WAN — отдельно не нужен. Проверь: `ip r`.

---

## 3. HQ-RTR / BR-RTR — базовая адресация (Eltex ESR)

> Имена портов смотри `show interfaces status`. Ниже шаблон — подставь свои `gi1/0/N`.

**HQ-RTR — интерфейс к ISP:**
```
configure
interface gigabitethernet 1/0/1
  ip address 172.16.1.2/28
  exit
ip route 0.0.0.0/0 172.16.1.1
exit
```

**BR-RTR — интерфейс к ISP:**
```
configure
interface gigabitethernet 1/0/1
  ip address 172.16.2.2/28
  exit
ip route 0.0.0.0/0 172.16.2.1
exit
```

> Включить интерфейсы в зону безопасности (Eltex без security-zone не маршрутизирует!):
```
configure
security-zone trusted
exit
interface gigabitethernet 1/0/1
  security-zone trusted
  exit
exit
```
**Сохранить конфиг Eltex (делай после каждого блока!):**
```
commit
confirm
```

---

## 4. Коммутация HQ + router-on-a-stick (HQ-RTR)

VLAN: **100** = HQ-SRV, **200** = HQ-CLI, **999** = управление. Один физ. порт, субинтерфейсы.

```
configure
interface gigabitethernet 1/0/2.100
  ip address 192.168.0.1/27
  security-zone trusted
  exit
interface gigabitethernet 1/0/2.200
  ip address 192.168.0.33/27
  security-zone trusted
  exit
interface gigabitethernet 1/0/2.999
  ip address 192.168.0.65/29
  security-zone trusted
  exit
exit
commit
confirm
```
> На виртуальном коммутаторе/портах назначь access-VLAN: HQ-SRV→100, HQ-CLI→200, транк к HQ-RTR. Зафиксируй настройку коммутации в отчёт.

---

## 5. Учётные записи

**Серверы HQ-SRV / BR-SRV (РедОС) — sshuser, UID 2026, sudo без пароля:**
```bash
sudo useradd -u 2026 -m -s /bin/bash sshuser
echo 'sshuser:P@ssw0rd' | sudo chpasswd
echo 'sshuser ALL=(ALL) NOPASSWD: ALL' | sudo tee /etc/sudoers.d/sshuser
sudo chmod 440 /etc/sudoers.d/sshuser
```

**Роутеры HQ-RTR / BR-RTR (Eltex) — net_admin, макс. привилегии:**
```
configure
username net_admin
  password P@ssw0rd
  privilege 15
  exit
exit
commit
confirm
```

---

## 6. Безопасный SSH на серверах (HQ-SRV / BR-SRV, РедОС)

Порт **2026**, только `sshuser`, **2** попытки, баннер.

```bash
sudo tee /etc/ssh/sshd_config.d/exam.conf >/dev/null <<'EOF'
Port 2026
MaxAuthTries 2
AllowUsers sshuser
Banner /etc/ssh/banner
EOF

echo 'Authorized access only' | sudo tee /etc/ssh/banner

# SELinux — разрешить нестандартный порт
sudo semanage port -a -t ssh_port_t -p tcp 2026 2>/dev/null || sudo semanage port -m -t ssh_port_t -p tcp 2026
# firewall
sudo firewall-cmd --permanent --add-port=2026/tcp
sudo firewall-cmd --reload

sudo systemctl restart sshd
```
> Проверь: `ss -tlnp | grep 2026`.

---

## 7. Туннель между офисами (GRE) — Eltex

`10.10.10.0/30`: HQ `.1`, BR `.2`. Source/destination = адреса в сторону ISP.

**HQ-RTR:**
```
configure
tunnel gre 1
  ip address 10.10.10.1/30
  local address 172.16.1.2
  remote address 172.16.2.2
  security-zone trusted
  enable
  exit
exit
commit
confirm
```
**BR-RTR:** то же, поменяй адреса:
```
  ip address 10.10.10.2/30
  local address 172.16.2.2
  remote address 172.16.1.2
```
> Проверь: `ping 10.10.10.2` (с HQ). Параметры туннеля → в отчёт.

---

## 8. Динамическая маршрутизация — OSPF, только на туннеле, с паролем

**HQ-RTR:**
```
configure
router ospf 1
  area 0.0.0.0
  enable
  exit
interface tunnel gre 1
  ip ospf instance 1
  ip ospf area 0.0.0.0
  ip ospf authentication message-digest
  ip ospf authentication-key ascii-text P@ssw0rd
  exit
exit
commit
confirm
```
> Анонсируй локальные сети офиса в OSPF (через `ip ospf` на нужных интерфейсах или network). На BR-RTR — аналогично. Сосед только один (через туннель) → требование «делятся только друг с другом» выполнено. Настройку и пароль → в отчёт. Проверка: `show ip ospf neighbors`.

---

## 9. NAT на роутерах + DHCP для HQ-CLI (Eltex)

**NAT в сторону ISP (HQ-RTR, аналогично BR-RTR):**
```
configure
nat source
  ruleset nat-internet
    rule 1
      match source-address <локальные сети>
      action source-nat interface
      enable
      exit
    exit
  exit
exit
commit
confirm
```
> Синтаксис NAT на ESR уточни `show nat source` — на разных прошивках отличается.

**DHCP для HQ-CLI (VLAN 200, сеть 192.168.0.32/27) на HQ-RTR:**
```
configure
ip dhcp-server
ip dhcp-server pool HQ-CLI
  network 192.168.0.32/27
  address-range 192.168.0.34-192.168.0.62
  default-router 192.168.0.33
  dns-server 192.168.0.2
  domain-name sirius-exam.org
  exit
exit
commit
confirm
```
> Адрес роутера `.33` исключён (range начинается с `.34`). Шлюз = HQ-RTR, DNS = HQ-SRV. Настройку → в отчёт.

**Проверка на HQ-CLI (РедОС):**
```bash
sudo nmcli con mod <соединение> ipv4.method auto
sudo nmcli con up <соединение>
ip a; ip r
```

---

## 10. DNS на HQ-SRV (РедОС, bind) + время

```bash
sudo dnf install -y bind bind-utils
```
Зона `sirius-exam.org` (прямая) и обратные зоны под `192.168.0.x` / `172.16.x`.
Записи по **Таблице 3**: hq-rtr, br-rtr, hq-srv, hq-cli, br-srv (+ A/PTR где указано),
ISP→HQ = `docker.sirius-exam.org`, ISP→BR = `web.sirius-exam.org`.

```bash
# /etc/named.conf — добавить:
#   forwarders { 77.88.8.7; 77.88.8.3; };
#   allow-query { any; };
sudo systemctl enable --now named
```
> Проверка: `dig @127.0.0.1 hq-rtr.sirius-exam.org` и `dig @127.0.0.1 -x <ip>`.

**Часовой пояс — на ВСЕХ устройствах (РедОС):**
```bash
sudo timedatectl set-timezone Europe/Moscow   # подставь свой по месту экзамена
```
**Eltex:**
```
configure
clock timezone gmt +3
exit
commit
confirm
```

---

## Финальная проверка связности

- `ping` ISP → HQ-RTR, BR-RTR (172.16.x.2)
- `ping` через туннель: HQ-RTR → 10.10.10.2
- `show ip ospf neighbors` — сосед `Full`
- HQ-CLI получил IP по DHCP, видит интернет и резолвит имена
- SSH на серверы по порту 2026 только под sshuser, виден баннер

> 🔴 Команды Eltex (NAT, DHCP, tunnel, OSPF) на разных прошивках ESR отличаются синтаксисом. Если строка не принимается — набери начало и нажми `?`, ESR подскажет варианты. Сверяйся с Прил_2 (инструкция для ТЭ).
